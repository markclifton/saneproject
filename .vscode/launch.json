{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Sandbox",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/bin/sandbox.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/",
            "environment": [
                {
                    "name": "PATH",
                    "value": "${workspaceFolder}/lib;${env:PATH}"
                }
            ],
            "console": "internalConsole",
            "preLaunchTask": ""
        }
    ]
}